import React, { Component } from 'react';

class MiniBlogDisplay extends Component {
  //might need to write blog posts in plain text/json
  //get title/meta info for the miniblog display and put in state,
  //when we want to see a specific article, pull from text file and parse into an article 
  render() {
    return (
      <div id='mini-blog-display'>
        
      </div>
    );
  }
}

export default MiniBlogDisplay;